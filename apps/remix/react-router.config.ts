import type { Config } from '@react-router/dev/config';

export default {
  basename: '/published-apps/17/160',
  appDirectory: 'app',
  routeDiscovery: {
    mode: 'initial',
  },
  ssr: true,
} satisfies Config;
