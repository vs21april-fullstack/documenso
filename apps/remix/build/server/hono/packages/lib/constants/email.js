import { env } from '../utils/env.js';

const FROM_ADDRESS = env('NEXT_PRIVATE_SMTP_FROM_ADDRESS') || 'noreply@documenso.com';
const FROM_NAME = env('NEXT_PRIVATE_SMTP_FROM_NAME') || 'Documenso';
const DOCUMENSO_INTERNAL_EMAIL = {
  name: FROM_NAME,
  address: FROM_ADDRESS
};
const EMAIL_VERIFICATION_STATE = {
  NOT_FOUND: 'NOT_FOUND',
  VERIFIED: 'VERIFIED',
  EXPIRED: 'EXPIRED',
  ALREADY_VERIFIED: 'ALREADY_VERIFIED'
};
const USER_SIGNUP_VERIFICATION_TOKEN_IDENTIFIER = 'confirmation-email';

export { DOCUMENSO_INTERNAL_EMAIL, EMAIL_VERIFICATION_STATE, FROM_ADDRESS, FROM_NAME, USER_SIGNUP_VERIFICATION_TOKEN_IDENTIFIER };
//# sourceMappingURL=email.js.map
